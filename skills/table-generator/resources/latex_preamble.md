Required packages by feature:

- booktabs output (top/mid/bottom rules):
  - \usepackage{booktabs}
- cell background colors (cellcolor:... styles):
  - \usepackage[table]{xcolor}
- tabularx backend:
  - \usepackage{tabularx}

The renderer uses \underline{} for underline, which does not require extra packages.
